# Claude Code

This project uses `AGENTS.md` as the only operator brief.

Read `AGENTS.md`. Do not create a second memory file, a home-directory dump, or a per-business novel in this repo. If a rule still binds, add three lines to the matching file under `.cursor/rules/`.
